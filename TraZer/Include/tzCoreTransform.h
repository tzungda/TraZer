#ifndef TZ_TRANSFORM_H
#define TZ_TRANSFORM_H

#include <vector>
using namespace std;

/*
The basic class for all the transformation interfaces
*/

class tzCoreTransform
{
	/*
public:
	tzTransform( ){};
	virtual ~tzTransform( ){};

private:
	// members
	tzObject*		  mParent;
	vector<tzObject*> mChildren;

public:
	// parent
	tzObject*				parent( ) const;
	void					setParent( tzObject* parent );

	// children
	int						numChildren( ) const;
	tzObject*				childByIndex( ) const;
	void					addChild( );
	*/
};

#endif